generator client {
  provider = "dart run orm"
  engineType = "flutter"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
} 

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}

model Macaco {
  id    Int     @id @default(autoincrement())
  cor String 
}